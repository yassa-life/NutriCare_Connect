import{useState}from"react";import{BellRing,Mail,MessageCircle,Send}from"lucide-react";
export function MessagingRemindersFeature(){const[messages,setMessages]=useState([{from:"Amal",text:"Can I replace brown rice with red rice?",mine:false},{from:"You",text:"Yes, keep the same portion size and log it with lunch.",mine:true}]);const[text,setText]=useState("");function send(e:React.FormEvent){e.preventDefault();if(text.trim()){setMessages(v=>[...v,{from:"You",text:text.trim(),mine:true}]);setText("")}}return <div className="stack-xl"><div className="page-heading"><div><span className="eyebrow">Module 05 · IT25103601</span><h1>Messages & reminders</h1><p>Secure, non-urgent conversations with reliable demo notifications.</p></div><span className="chip active"><BellRing size={13}/> Reminder scheduler active</span></div><div className="feature-grid"><section className="panel"><div className="panel-title"><div><span className="eyebrow">Amal Perera</span><h2>Care conversation</h2></div><MessageCircle size={20}/></div><div className="list">{messages.map((m,i)=><div className="list-item" key={i} style={{marginLeft:m.mine?"14%":0,background:m.mine?"#e8f3ed":"white"}}><span className="avatar">{m.from[0]}</span><span className="grow"><strong>{m.from}</strong><p>{m.text}</p><small>10:{32+i*4} AM · Delivered</small></span></div>)}</div><form onSubmit={send} style={{display:"flex",gap:8,marginTop:14}}><input aria-label="Message" value={text} onChange={e=>setText(e.target.value)} placeholder="Write a non-urgent message…" style={{flex:1,border:"1px solid #d9e3dd",borderRadius:10,padding:11}}/><button className="primary" aria-label="Send message"><Send size={17}/></button></form></section><section className="panel"><div className="panel-title"><div><span className="eyebrow">Delivery queue</span><h2>Automated reminders</h2></div><Mail size={20}/></div><div className="list">{[["Appointment tomorrow","SMS + email","Scheduled"],["Diet plan updated","In-app + email","Delivered"],["Follow-up overdue","SMS","Retry in 5 min"]].map(([title,channel,status])=><div className="list-item" key={title}><span className="avatar"><BellRing size={15}/></span><span className="grow"><strong>{title}</strong><small>{channel} · simulated delivery</small></span><span className={status==="Delivered"?"status confirmed":"status pending"}>{status}</span></div>)}</div><div className="success-note">Email and SMS are simulated. Delivery attempts and retry status are saved for the demo.</div></section></div></div>}

