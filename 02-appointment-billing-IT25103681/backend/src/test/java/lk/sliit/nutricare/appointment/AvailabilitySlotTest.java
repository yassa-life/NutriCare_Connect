package lk.sliit.nutricare.appointment;import static org.junit.jupiter.api.Assertions.*;import java.time.*;import java.util.UUID;import org.junit.jupiter.api.Test;class AvailabilitySlotTest{@Test void cannotHoldSlotTwice(){var s=new AvailabilitySlot(UUID.randomUUID(),LocalDateTime.now().plusDays(1));s.hold();assertThrows(IllegalStateException.class,s::hold);}}
