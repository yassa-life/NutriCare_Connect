package lk.sliit.nutricare.appointment;import static org.junit.jupiter.api.Assertions.*;import java.time.*;import org.junit.jupiter.api.Test;class AvailabilitySlotTest{@Test void cannotHoldSlotTwice(){var s=new AvailabilitySlot("D001",LocalDateTime.now().plusDays(1));s.hold();assertThrows(IllegalStateException.class,s::hold);}}
