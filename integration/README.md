# Integrated application

`backend` is the Spring Boot composition module that loads all six feature jars. `frontend` is the shared Vite shell that imports one React package from each member folder. Run both from the repository root using the instructions in the main README.

